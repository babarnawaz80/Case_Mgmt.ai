rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Profile photos: authenticated user can read/write only their own folder
    match /profile_photos/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.auth.uid == userId
                   && request.resource.size < 5 * 1024 * 1024   // 5 MB max
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null && request.auth.uid == userId;
    }

    // Organization logos: any authenticated user can read; only org members can write
    match /org_logos/{orgId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024   // 5 MB max
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }

    // Individual (person) photos: authenticated users can read; any authenticated user can write
    match /individual_photos/{individualId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024   // 5 MB max
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }

    // All other paths: deny by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
